Tutorial:

run songsearch.py from cmd to initialize the databases or to search for stuff directly from console

After having initialized the db (songsearch.py can now be stopped), start webapp.py and go to the website that is displayed in the console.